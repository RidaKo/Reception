{% extends 'base.html.twig' %}

{% block title %}Reservations{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
                    {% if error is defined %}
                    <h1> There is a problem with your MESSENGER_DSN key in .env </h1>
                    {% endif %}
        <div class="col-md-6">
            <div class="card text-center">
                <div class="card-body">
                    {% if cancelled is defined %}
                        <p class="alert alert-danger">Visit has been canceled.</p>
                    {% endif %}
                    
                    <h1 class="display-4">Make a Reservation</h1>
                    <form action="{{ path('app_reserved') }}" method="post" class="mt-4">
                        <div class="form-group">
                            <input type="email" name="email" class="form-control" placeholder="Enter your email" required>
                        </div>
                        <button type="submit" class="btn btn-primary btn-block">Submit</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

